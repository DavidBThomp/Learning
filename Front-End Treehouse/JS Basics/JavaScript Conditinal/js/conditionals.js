const answer = prompt("Which planet is closest to the sun?");

if (answer.toUpperCase() === 'MERCURY') {
    console.log("That's correct!");
} else {
    console.log("That is incorrect.");
}

// Strict equality operator ===
// If (condition){If true run this}
//   else {run if not true}

// Equality operator (==) Compares Value, using this is less common than strict
// Strict equality operator (===) Compares value and type
// Logical not Operator (!)
// Strict Inequality Operator (!==)

// And (&&)
// Or (||)